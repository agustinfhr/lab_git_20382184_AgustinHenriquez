#lang racket
;TDA RACKETGRAM
;lista cuentas
;lista publicaciones


; Representación TDA Racketgram => (usuarios, publicaciones)

; Representación TDA Usuario => (nombre, contraseña, tipo, seguidores, id_publicacion)

; Representación TDA Publicaciones => (id_publicacion, autor, texto, likes, comentarios, fecha, hora)

; Representación TDA Comentario => (texto id_publicacion autor tiempo)

; Representación TDA Tiempo => (fecha, hora)



